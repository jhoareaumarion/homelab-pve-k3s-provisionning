apiVersion: cilium.io/v2alpha1
kind: CiliumLoadBalancerIPPool
metadata:
  name: vlan20-pool
spec:
  blocks:
  - start: "10.10.20.151"
    stop: "10.10.20.199"
  serviceSelector:
    matchExpressions:
    - key: gateway.networking.k8s.io/gateway-name
      operator: In
      values: ["argocd-gateway"]