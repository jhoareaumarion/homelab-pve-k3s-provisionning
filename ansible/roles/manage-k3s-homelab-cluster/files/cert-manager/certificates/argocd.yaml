apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-certificate
  namespace: argocd
spec:
  dnsNames:
    - argocd.hoareau-marion.eu
  issuerRef:
    name: letsencrypt  
    kind: ClusterIssuer
  secretName: argocd-hoareau-marion-eu-tls 
