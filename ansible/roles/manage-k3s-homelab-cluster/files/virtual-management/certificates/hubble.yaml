apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: hubble-certificate
  namespace: virtual-management
spec:
  dnsNames:
    - hubble.hoareau-marion.eu
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  secretName: hubble-hoareau-marion-eu-tls

